package com.aguskoll.data.repositories

import com.aguskoll.domain.repositories.IMyRepository

/**
 * Example of where to put the repositories implementations to decouple domain from data
 */
class MyRepository : IMyRepository
